# 최종 로드맵 — 2026/8/24-2026/10/12

**최종 목표**: 좀비 영상 입력 → 유니티 에셋(외형+리깅+동작) 출력의 완전 자동 파이프라인으로 에셋 양산.

| 마일스톤 | 기한 | 완료 조건 |
| :---- | :---- | :---- |
| **M1. 수동 파이프라인 완성** | **8/31** | TRELLIS 외형 + SMPL 리깅 + WHAM 동작 좀비가 Unity에서 재생됨. README에 통합 데모 GIF. 전 단계 절차가 스크립트+문서로 재현 가능 |
| **M2. 자동 파이프라인 완성** | **9/30** | 영상 입력 → 에셋 출력이 원커맨드로 실행. 게이트(G0-G4) 판정·재시도 작동. Docker로 환경 재현 가능 |
| **M3. 양산 + 최종 점검** | **10/12** | 자동 파이프라인으로 좀비 에셋 4종+ 생산 완료. 최종 문서·데모 정리 |

---

## W1. 8/24-8/28 — 웨이트 전이 결판 & M1 마감 주

- [x] 8/24/월: 2단계 전이(개정 transfer_weights.py) 재실행 → 포즈 시험(R_Elbow 부채막 소멸, Spine2 자락 추종, R_Hip 유지) → **EH-104 완료 판정**; 스크립트 커밋 + PROJECT_STATUS 기록 (2단계 전이 설계 판단 포함)
- [x] 8/25/화: **4단계 — WHAM pose 재생 검증** 스크립트 실행 (69프레임을 SMPL_rig에 적용, 좀비가 영상 동작대로 움직이는지) → 08-24 완료; 재생 영상 확보, 원본 영상과 나란히 비교 → 08-24 완료; Unity 반입 — FBX export(동작 포함), Humanoid 설정, 텍스처 연결 → 08-24 완료
- [x] 8/26/수: 통합 데모 GIF(원본 영상 vs Unity 좀비 나란히) → README "통합" 섹션 채움 → 08-24 완료; **feature/smpl-rigging → main PR 머지** (리깅 전체 검증 완료 시점) → 08-24 완료; SAM2 RunPod 이전 — 볼륨에 sam2 환경 설치, try6 → `scripts/run_sam2.py` 정제 → 08-25 완료
- [x] 8/27/목: SAM2 검증(기존 영상 마스크 재현, Colab 결과와 대조) + 커밋 → 08-25 완료; 수동 파이프라인 runbook 작성 — 영상→에셋 전 단계 명령·순서 문서화 (`docs/RUNBOOK.md`) → 08-27 완료
- [x] 8/28/금: 좀비 영상 4종 소싱 + 입력 조건 검증(90-600프레임, 팔 벌린 키프레임 등 — G0/G1 수동 적용) → 08-27 완료; **M1 점검** — 완료 조건 대조, 미달 항목 주말 처리 계획 → 08-28 완료; Jira 다음 주 카드 발행 (좀비 4종 소싱 반영) → 08-28 완료 (W2 카드 4건 발행)

**M1 체크리스트**: ☑ WHAM 동작 좀비 Unity 재생 ☑ 통합 GIF ☑ main 머지 ☑ runbook ☑ SAM2 레포 반입 — **8/27 완료 (목표 8/31 대비 4일 조기)**

\[8/30 or 8/31 멘토링 회의. 작업리스트 확정\]

<details><summary>W1 계획 변경 이력</summary>

- **버퍼 (밀린 항목 처리 또는 휴식)** — 조기 달성으로 소진 없음.

</details>

## W2. 8/31-9/4 — 로컬 기반 완성

- [x] 8/31/월-9/1/화: TRELLIS 로컬 설치 (RunPod 볼륨, setup_trellis.sh 수정·검증) — Space 의존 제거. **자동화의 전제 조건** → 09-02 완료
- [x] 9/2/수: TRELLIS 로컬 생성 결과를 Space 결과와 품질 대조 → 09-02 완료 (동등 판정)
- [x] 9/3/목: 전 단계 스크립트 CLI 표준화 — 입출력 경로 규약(`data/00_raw`-`07_unity`) 통일, config.yaml 연결 → 09-03 완료 (config.yaml 은 신규 작성, 스크립트 연결은 W4 게이트 작업 때)
- [ ] 9/4/금: 수동 파이프라인으로 **좀비 2호 제작** (runbook 검증 겸 양산 리허설) + 주간 마무리 → **부분 달성**: S2-S5 + 5-1 관통, AI 생성 영상 WHAM 적합성 판정 완료

<details><summary>W2 계획 변경 이력</summary>

- 9/4/금 좀비 2호 제작이 부분 달성에 그쳐 **에셋 완성(5-2-6 리깅 사슬)을 W3 월로 이월**했다 — WHAM 환경 복구에 예상 밖 시간이 들었다.

</details>

## W3. 9/7-9/11 — 이월 완료 + 라이선스 관문 분기

- [x] 9/7/월: **리깅 사슬 이월분 완주** — 좀비 2호(zombie1) Unity 반입·재생 확인.
      정렬 IoU 0.797 / 관절-메쉬 거리 0.0293 / 웨이트 무배정 0%. 어긋남 8-11 수정, 리허설 종료
- [x] 9/8/화: **TRELLIS.2 LICENSE 원문 확인** — 코드 LICENSE(MIT 전문, sell 포함)와 모델 카드
      (`license: mit`) 모두 MIT. 프로젝트 페이지의 "research purposes" 문구는 페이지 자료 대상
      일반 고지이며 산출물에 첨부된 라이선스가 우선한다고 판단. 1세대(현재 사용 중)와 조건이
      같다. **→ 분기 A 채택** (원문·판단 근거는 EH-218 코멘트)

### 확정 계획 (분기 A — 상용 가능)

> **현황 요약** — ① TRELLIS.2 전환 (EH-218) ✅ **완료** (9/8-9/9) ·
> ② 1080p 재생성 (EH-220) ⚠ **입력이 720p 로 확인돼 목적 재정의** — 4샘플 일반화 검증으로
> 수행(9/10), 1080p 는 Veo API 경로로 이월 ·
> ③ 표준 해상도 **1024 + force-dielectric 확정** (9/11) ·
> ④ 게이트 구현 **W4 로 이동**

- [x] ① **TRELLIS.2 전환 완료** (09-08-09, EH-218): 환경 구축 → 스모크(512·1024) → 통제 실험 →
      zombie1 S4-Unity 관통까지 마쳤다. 라이선스 MIT 확인으로 분기 A 확정.
      **A100 불필요 — RTX 4090(24GB)으로 확정**(peak VRAM 512 2.6GB / 1024 3.1GB).
      통제 실험으로 "부풀음은 밀도가 아니라 표현 체계 탓" 논증 완결(1세대 정점 22배로 올려도 재현).
      관통 수치: 스케일 0.5892(상수 세대 불변 3번째 검증) / 관절거리 0.0292 / 웨이트 무배정 0%,
      1차 수신 93.2%, 폴백 1.5%, 27초 — 고밀도가 리깅에 이득이라는 역전 실측.
      설치 수정 3건(EXDEV·import명·transformers 핀)은 모두 우리 스크립트 결함이었다
- [x] ①-잔여: **1024 metallic 규명 종결**(9/10 — 해상도·입력 둘 다 변수) / **표준 해상도 결정 완료**
      (9/11 — 1024 + force-dielectric). **1536_cascade 실측**만 남아 A100 재고 시로 이월
- [x] ①-후속: 볼륨·미러를 `03_trellis/gen1·gen2` 세대 구조로 이동 완료 (9/10)
- [ ] ①-후속: 볼륨·미러를 `03_trellis/gen1·gen2` 세대 구조로 이동 (다음 Pod 세션 첫 작업)
- [ ] ② **1080p 재생성** (EH-220) — ① 완료(9/9)로 착수 가능, **W3 9/10/목 예정**.
      진단 목적이 아니다 — 원인 분리는 9/7 실증으로 끝났다 (얼굴 = 해상도, 부풀음 = 1세대 한계)
> **정정** — "1024 는 512 의 2.4배 시간" 은 zombie1 단일 샘플 관측이고 일반화되지 않는다.
> walker 는 512 가 79.6s, 1024 가 35.7s 로 **1024 가 더 빨랐다**. 같은 512 에서도 22.1s ↔ 79.6s 로
> 3.6배 차이난다 — 생성 시간은 해상도가 아니라 형상 복잡도에 좌우된다. 해상도 결정의 시간
> 근거는 고정 배수가 아니라 범위로 다룬다.

- Docker 는 W4 전반, 게이트 ③(G1a 키프레임 자동 선정 · G2 메쉬 품질 · G3 재투영 PCK 등
  자동 판정 — 수동 육안 확인을 수치 판정으로 대체)은 W4 후반으로
  (리허설에서 판정 재료를 확보해 압축 가능)
- [x] **9/10/목**: Pod 세션 — `gen1`/`gen2` 폴더 이동 완료, params.json 미러→볼륨 동기화.
      1080p 입력이 **720p 로 판명**돼 목적을 4샘플 일반화 검증으로 재정의했다.
      SAM2 4종(2종 실패 — 단일 포인트 한계) · TRELLIS.2 512 3종 생성 · **metallic 규명 종결**
      (해상도·입력 둘 다 변수). 게이트 설계 재료 5건 확보. 1536 은 4090 이라 건너뜀
- [x] **9/11/금**: **표준 해상도 1024 + `--force-dielectric` 확정**. 수치로는 512 가 유리했으나
      육안 재검토로 512 의 형상 융합·색 표백(56-69%)이 드러나 결론이 뒤집혔다.
      1024 관통 검증(스케일 0.5914 — 상수 5번째 검증, 폴백 3.67%, 포즈 시험 통과) + 주간 마무리 +
      **W4 재계획** (Docker·게이트·발표 준비 배치) — W3 는 "Docker 는 W4 전반" 을 예고하는데
      W4 절 요일 배치는 재편 전 계획 그대로라 Docker 자리가 없다. 게이트 5일 + Docker +
      중간 발표(9/18-19, 준비 이틀)가 한 주에 들어가지 않는다

<details><summary>분기 B (연구용 한정) — 라이선스 확인 결과 A 채택(9/8)으로 미실행</summary>

- ① **전환 동결** — EH-218 에 SMPL 상업 라이선스 이슈와 같은 줄로 기록해 배포 결정 시점에 함께 재검토
- W3 = **Docker 화 + ② 1080p 재생성 병행** (Dockerfile: wham, sam2, trellis, blender)
- 게이트 ③ 은 W4 유지

</details>

> 상용 출시 단계에서는 전체 라이선스를 정식 검토한다 — SMPL 상업 라이선스 이슈와 같은 표(리스크와 대응)에서 관리.

## W4. 9/14-9/18 — 게이트 전반 + Docker + 중간 발표

게이트 5일 + Docker + 발표가 한 주에 들어가지 않으므로 **G1m·G3·G4 를 W5 로 옮기고**
입력·외형 쪽 게이트만 W4 에서 구현한다. W3 에서 확보한 실측 지표를 그대로 임계값으로 쓴다.

- [x] **9/14/월-9/15/화**: **게이트 5종 완료 — 예정보다 하루 단축**
      `gate_s2.py` · `gate_g2.py` · `gate_g2r.py` 신규, `run_sam2.py` 에 `--keyframe-min-gap`.
      오탐 시험은 gate_s2 볼륨 실데이터 **9/9**, G2·G2r **16/16**.
      - G1a **자세 항은 미구현 — 재설계 필요**. 실루엣 기반이 기각됐다. solidity 가
        f80 0.514 / f222 0.644 인데 실제로 팔이 몸통에 융합된 쪽은 f80 이다 —
        실루엣의 오목함은 "팔이 옆에 있다" 와 "팔이 앞에 있다" 를 구분하지 못한다.
        2D 키포인트(팔-몸통 이미지 공간 겹침, 투영 팔 길이 대 기대 길이 비)가 필요하다.
        간격 제약만으로 f222 가 후보에 들어와 실용적으로는 동작한다
      - 연결 요소 수는 **품질 지표로 쓰지 않는다** — f80 이 1개, f222 가 102개인데
        f80 의 "깨끗함" 이 곧 융합이라 방향이 거꾸로 붙는다
- [x] **9/16/수**: **문서 정리 + Docker 선행 조사**
      CLAUDE.md 인용 검증 규칙, RUNBOOK 게이트 절 신설·1절 레포 최신화,
      PROJECT_STATUS 9/14-15, 1536 시각 확인. Docker 조사 결과는 목요일 계획에 반영
- [ ] **9/17/목**: **`setup_sam2.sh` 복원 + 시범 Dockerfile 1종**
      4종 일괄은 두 가지 이유로 무리다. ① `sam2` 는 setup 스크립트가 없어(수동 설치)
      "번역" 할 원본이 없다 — Pod 에서 환경을 역추적해 복원하는 것이 선행이고, 그 자체가
      재현 절차 확보라는 독립적 가치를 갖는다. ② 볼륨의 `micromamba` 가 64GB 라
      이미지화 타당성부터 재야 한다. 순서: sam2 역추적 → Pod 의 `docker` 가용성 실측
      → 환경별 크기 측정 → 타당하면 `trellis2` 시범 1종
- [ ] **9/18/금**: 발표 대본 · 예상 질문 대비
- [ ] **9/19/토**: **중간 발표**

G1m(동작 클립 평가) · G3(재투영 PCK) · G4(통합 평가)는 **W5 로 이동**.

> **해상도 각주** — 1536_cascade 실측(9/15, A100)으로 비용 곡선이 닫혔다. 표준은 **1024 유지**다.
> 1536 은 시간 1.9배(492s vs 262s)에 정점 +8%, 면 수는 오히려 감소한다(`decimation_target` 상한).
> 품질은 동일하지 않고 **부위별 트레이드오프**다 — 정면과 찢어진 옷은 1536 이 낫고(가장자리가
> 날카롭고 얼룩이 또렷하다), 후측면의 머리카락·팔다리는 1536 이 열화된다. cascade 가 입력에
> 보이지 않는 영역에서 불안정할 가능성이 있다. **1536 은 케이스별 옵션**으로 남긴다 —
> 정면 클로즈업이 중요한 샘플에 한해 고려한다.

## W5. 9/21-9/25 — 오케스트레이터 + 통합 시험

- [ ] 9/21/월-9/22/화: 오케스트레이터 연결 — 단계 실행, 게이트 판정, 실패 원인별 재진입, 횟수 제한  
- [ ] 9/23/수-9/24/목: **E2E 자동 실행 1회전** (좀비 1종, 영상→에셋 무개입) — 실패 지점 수정 반복  
- [ ] 9/25/금: 재시도 경로 시험 (일부러 나쁜 입력 → 게이트 불합격 → 재진입 확인), **M2 점검**

**M2 체크리스트**: □ 원커맨드 E2E □ 게이트 전체 작동 □ 재시도 검증 □ Docker 재현 □ README 자동화 사용법

## W6. 9/28-10/2 — 양산 1차

- [ ] 9/28/월-9/30/수: 자동 파이프라인으로 **좀비 4종 생산** (W1에서 소싱한 영상)  
- [ ] 10/1/목-10/2/금: 품질 이슈 대응 — 게이트 임계값 튜닝, 실패 케이스 분석·수정

++  유니티 LOD 최적화 & QA 작업 (10/1-10/12)

## W7. 10/5-10/9 — 양산 2차 + 최종

- [ ] 10/5/월-10/6/화: 추가 에셋 생산 / 미비 좀비 재생산  
- [ ] 10/7/수-10/8/목: 최종 정리 — README(전 데모 GIF), PROJECT_STATUS, 구조도 최신화, 발표 자료 초안  
- [ ] 10/9/금: 전체 리허설 (클린 환경에서 파이프라인 1회전)  
- [ ] **10/12/월 최종 점검**: M1-M3 완료 조건 전수 확인

---

## 리스크와 대응 (계획에 반영된 버퍼)

| 리스크 | 징후 | 대응 |
| :---- | :---- | :---- |
| 웨이트 전이 품질 미달 | W0 포즈 시험 실패 | 공식 SMPL 웨이트 추출·교체 (--weights 경로 이미 마련). 최대 +2일 |
| SMPL prior의 좀비 자세 왜곡 | W0-W1 재생 검증에서 자세 뭉개짐 | 극단 자세 프레임 제외, 실전 영상 조건 강화. G1m에 반영 |
| TRELLIS 로컬 설치 난항 | W2 화요일까지 미해결 | W2 금 좀비 2호를 Space로 대체하고 설치는 W3와 병행 |
| Docker 이미지 용량·빌드 시간 | W3 | 볼륨 환경 그대로 두고 이미지화는 최소 레이어만. 완벽보다 재현 우선 |
| 좀비 영상 소싱 지연 | W1 금 | 직접 촬영으로 대체 (조건 충족 셀프 영상) |
| RunPod 비용 | 잔액 $32 | 자동화 시험은 CPU Pod 우선, GPU는 실행 단계만. 주당 약 $5 상한 |
| SMPL 상업 라이선스 | 배포 결정 시점 | 소마 내 시연은 연구용 범위. 배포 전 Meshcapade 검토 (10월 초 확인) |

## 운영 규칙

- 매주 **금요일 주간 마무리**: 이 문서 체크 갱신 + PROJECT_STATUS 기록 + 다음 주 Jira 카드 발행  
- 지연 발생 시: 다음 주 계획을 미루는 게 아니라 **해당 주 버퍼(금·주말)에서 흡수**, 흡수 불가 시 M 기한 기준으로 범위 축소 (예: 좀비 4종 → 2종)  
- 이 파일은 `docs/ROADMAP.md`로 커밋, 체크는 GitHub에서 직접 또는 Claude Code로

